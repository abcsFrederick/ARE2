sudo dnf install libcurl-devel openssl-devel platform-python-devel openslide python3-openslide

manage.py makemigrations migrate

# For celery and rabbitmq
EPEL install
sudo dnf install https://dl.fedoraproject.org/pub/epel/epel-release-latest-8.noarch.rpm
sudo yum -y update
curl -s https://packagecloud.io/install/repositories/rabbitmq/rabbitmq-server/script.rpm.sh | sudo bash
curl -s https://packagecloud.io/install/repositories/rabbitmq/erlang/script.rpm.sh | sudo bash

Install RabbitMQ on RHEL 8
#Add A Yum repository with RabbitMQ packages 
curl -s https://packagecloud.io/install/repositories/rabbitmq/rabbitmq-server/script.rpm.sh | sudo bash
Edit # In /etc/yum.repos.d/rabbitmq.repo
(offical document)

yum update -y
yum -q makecache -y --disablerepo='*' --enablerepo='rabbitmq_erlang' --enablerepo='rabbitmq_server'
yum install socat logrotate -y
yum install --repo rabbitmq_erlang --repo rabbitmq_server erlang rabbitmq-server -y

# Install redis service
sudo yum install redis